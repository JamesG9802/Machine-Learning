use dragon_math;
use dragon_math::matrix::Matrix;
use crate::model as model;
use model::Model as Model;
/// <summary>
/// A model for performing perceptron training.
/// </summary>
pub struct Perceptron {
    pub model: Model,
}
impl Perceptron {
    //pub fn new(input_size: usize) -> Perceptron {
    //    let model: Model = Model::new(input_size, linear_regression_predict);
    //    return Perceptron { model: model };
    //}
}
